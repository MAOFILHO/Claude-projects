# Claude-artifacts
Artifacts are interactive, editable, shareable workspaces that appear in a side panel next to your chat. Instead of pasting a 200-line code block into chat that scrolls off-screen, Claude generates the code into a dedicated panel where it renders live, you can preview it, edit it, and share a public link to it, all without leaving Claude.ai.
